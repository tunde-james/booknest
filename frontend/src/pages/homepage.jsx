import BookList from '../components/book-list';
import Hero from '../components/hero';

const Homepage = () => {
  return (
    <main>
      <Hero />
      <BookList />
    </main>
  );
};

export default Homepage;
